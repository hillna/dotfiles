#!/bin/bash

for i in `cat ~/dev/Olark/puppet/manifests/host_ips.pp |
	grep rackspace |
	sed 's/,/\ /g' |
	sed 's/\[/\ /g'| 
	awk '{print $1}'|
	sed "s/'//g"`;
do 
	echo "$i.olark.net"
	ssh -oStrictHostKeyChecking=no $i.olark.net 'echo authorized'
done

