#!/bin/sh

if [ $(pgrep -cx panel) -gt 0 ] ; then
    pkill lxpanel &
    pkill panel &
    bspc config top_padding 0
else
    pkill lxpanel &
    $HOME/.config/bspwm/panel/panel &
    sleep 1s && lxpanel &
fi

